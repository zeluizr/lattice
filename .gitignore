# dependencies
node_modules/

# build output (regenerated by `npm run build` / prepublishOnly)
dist/

# prebuilt native helper (regenerated by `npm run build:native` / prepublishOnly)
prebuilds/

# packaged tarballs
*.tgz

# secrets / local config
.env
.env.local

# misc
.DS_Store
*.log
npm-debug.log*

# spurious pnpm files (this project uses npm + package-lock.json)
pnpm-lock.yaml
pnpm-workspace.yaml
